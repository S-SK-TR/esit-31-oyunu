import React, { useState } from 'react'; import './App.css'; const App = () => { const [numbers, setNumbers] = useState(Array(31).fill(false)); const [currentPlayer, setCurrentPlayer] = useState(1); const [gameOver, setGameOver] = useState(false); const [winner, setWinner] = useState(null); const handleNumberClick = (index) => { if (numbers[index] || gameOver) return; const newNumbers = [...numbers]; newNumbers[index] = true; setNumbers(newNumbers); const selectedNumbers = newNumbers.filter(num => num).length; if (selectedNumbers === 31) { setGameOver(true); setWinner(currentPlayer); } else { setCurrentPlayer(currentPlayer === 1 ? 2 : 1); } }; const resetGame = () => { setNumbers(Array(31).fill(false)); setCurrentPlayer(1); setGameOver(false); setWinner(null); }; return ( <div className="game-container"> <h1>Eşit 31</h1> <div className="player-info"> <h2>Oyuncu {currentPlayer} sırası</h2> {gameOver && <h2>Oyuncu {winner} kazandı!</h2>} </div> <div className="number-grid"> {numbers.map((isSelected, index) => ( <div key={index} className={`number ${isSelected ? 'selected' : ''}`} onClick={() => handleNumberClick(index)}> {index + 1} </div> ))} </div> <button className="reset-button" onClick={resetGame}>Yeniden Başlat</button> </div> ); }; export default App;